<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.skywardblocker">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SYSTEM_EXEMPTED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- Main status activity -->
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Lightweight Foreground Service holding dynamic PackageChangeReceiver (System Exempted for Device Owner) -->
        <service
            android:name=".blocking.AppMonitorService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="systemExempted|specialUse">
            <property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE" android:value="device_policy_enforcement" />
        </service>

        <!-- Resumes monitoring after reboot or APK replacement without user intervention -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Detects new app installs and uninstalls to keep blocklists updated -->
        <receiver
            android:name=".receiver.PackageChangeReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_ADDED" />
                <action android:name="android.intent.action.PACKAGE_FULLY_REMOVED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>

        <!-- Device Admin Receiver for Device Owner mode (ADB distribution) -->
        <receiver
            android:name=".admin.SkywardDeviceAdmin"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_policies" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <!-- Receiver for desktop companion app & debug ADB commands (e.g. self-clearing Device Owner) -->
        <receiver
            android:name=".receiver.AdbCommandReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.example.skywardblocker.CLEAR_OWNER" />
                <action android:name="com.example.skywardblocker.PUSH_CONFIG" />
                <action android:name="com.example.skywardblocker.PUSH_SCHEDULE" />
                <action android:name="com.example.skywardblocker.FINALIZE_SETUP" />
            </intent-filter>
        </receiver>

        <!-- Fires the daily locked-hours lock/unlock transition, scheduled via AlarmManager -->
        <receiver
            android:name=".schedule.ScheduleAlarmReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.example.skywardblocker.ACTION_SCHEDULE_BOUNDARY" />
            </intent-filter>
        </receiver>

    </application>
</manifest>